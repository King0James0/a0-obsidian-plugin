name: obsidian
title: Obsidian
description: Gives Agent Zero its own Obsidian vault — exposes the obsidian CLI so the agent can read, create, search, and organize markdown notes, daily notes, and tasks, plus an Obsidian Canvas surface to view the live app (graph view, editor). Vault persists outside the plugin.
version: 1.3.3
settings_sections: []
per_project_config: false
per_agent_config: false
always_enabled: false
